# ПРОМТЕХНОСФЕРА — Финансовая платформа

Многопользовательская платформа управленческого учёта: начисления и движение
денег отдельно, ДДС/ОПиУ/управленческий баланс, дебиторка/кредиторка,
зарплата, интеграции с 1С и Битрикс24, закрытие периодов. Источник истины —
серверная база данных PostgreSQL.

## Стек

- Next.js 16 (App Router) + TypeScript, React 19
- PostgreSQL + Prisma ORM (версионируемые миграции)
- Аутентификация: сессии на подписанных JWT (httpOnly cookie), bcrypt
- Server Actions как серверный API-слой; Route Handlers — для интеграций/экспорта
- Vitest — модульные тесты

## Запуск проекта локально

1. Установите зависимости:

   ```bash
   npm install
   ```

2. Скопируйте `.env.example` в `.env` и укажите строку подключения к
   PostgreSQL и `AUTH_SECRET` (длинная случайная строка).

3. Примените миграции и заполните начальные справочники:

   ```bash
   npm run db:migrate
   npm run db:seed
   ```

   Скрипт `db:seed` создаст роли, права и первого администратора (email
   берётся из `SEED_ADMIN_EMAIL`, по умолчанию используется адрес из
   переменной окружения). Временный пароль будет выведен один раз в консоль.

4. Запустите сервер разработки:

   ```bash
   npm run dev
   ```

   Приложение доступно на http://localhost:3000 и сразу открывает экран входа.

5. Тесты:

   ```bash
   npm test
   ```

## Развёртывание через Docker

Есть `Dockerfile` (многоступенчатая сборка, standalone-режим Next.js) и
`docker-compose.yml` (приложение + PostgreSQL). **Написано, но не
запускалось** в этой среде разработки — Docker здесь не установлен (сборка
и тесты приложения проверялись напрямую через Node.js/PostgreSQL на
хосте). Перед реальным использованием проверьте `docker compose up --build`
на своей машине.

Порядок:

```bash
export AUTH_SECRET=$(openssl rand -base64 32)
docker compose up -d postgres
# примените миграции и заполните справочники с хоста, указав на
# опубликованный порт контейнера (localhost:5432) — сознательно не
# выполняется автоматически при старте контейнера приложения, см. раздел
# «Правила выполнения задачи», п.8 ТЗ
DATABASE_URL="postgresql://promtehnosfera:promtehnosfera@localhost:5432/promtehnosfera" npm run db:migrate
DATABASE_URL="postgresql://promtehnosfera:promtehnosfera@localhost:5432/promtehnosfera" npm run db:seed
docker compose up -d app
```

Обязательные переменные окружения для продакшена: `DATABASE_URL`,
`AUTH_SECRET` (длинная случайная строка — используется и для подписи
сессий, и как ключ шифрования секретов интеграций, см. Этап 7).

## Реализовано (Этап 1: Фундамент)

- Полная схема данных в `prisma/schema.prisma`, покрывающая все домены из
  ТЗ: identity/access, master-data, accruals, cash-management,
  payments-matching, payroll, integrations, reporting, financial-modeling,
  period-close, audit.
- Аутентификация по email/паролю, сессии на JWT в httpOnly cookie.
- RBAC: 8 системных ролей, права настраиваются через
  «Администрирование → Роли и права» (создание ролей, изменение набора
  прав). Пользователи управляются через «Администрирование → Пользователи».
- Разграничение видимости по организациям/подразделениям/проектам
  (`UserOrganizationAccess`, `UserDepartmentAccess`, `UserProjectAccess`,
  `src/lib/access-scope.ts`) подключено к фильтрации данных. Принцип: «явное
  назначение — ограничение» — пока администратор не отметил пользователю ни
  одной организации/подразделения/проекта в «Администрирование →
  Пользователи → [пользователь] → Ограничение видимости», этот разрез
  остаётся неограниченным; полный администратор (`admin.full`) всегда видит
  всё в обход назначений. Подключено к: дашборду, списку документов
  начисления, банку/кассе, сотрудникам, расчётам зарплаты и всем пяти
  отчётам (ДДС, ОПиУ, управленческий баланс, маржинальность, дебиторка и
  кредиторка — включая выпадающий список организаций в фильтрах балансового
  отчёта и отчёта по задолженности). **Не подключено**: выгрузка в Excel
  (`/api/reports/export`), снимки закрытия периода и справочники-выпадашки
  в формах создания документов (создавая начисление, пользователь пока
  может выбрать любую организацию из списочника, а не только «свою») — эти
  места по-прежнему видят данные без ограничения по разрезу.
- Журнал аудита: все создания/изменения/архивирования справочников,
  вход/выход, закрытие и открытие периодов пишутся в `audit_log` и видны в
  «Администрирование → Журнал аудита».
- Учётные периоды: создание, ручное закрытие и повторное открытие с
  фиксацией пользователя и времени. Полный контрольный лист закрытия
  (проверка несопоставленных операций, незакрытых зарплатных расчётов и
  т.д. — раздел 17 ТЗ) — **не реализован**, это Этап 7.
- Справочники с реальным CRUD (создание, редактирование, архивирование —
  без физического удаления) и аудитом изменений:
  организации и ИП, подразделения, ЦФО, проекты, направления, продукты и
  услуги, контрагенты, договоры, статьи ДДС, статьи ОПиУ, статьи баланса.
  Начальные справочники (подразделения, направления, статьи) засеяны по
  тексту ТЗ.
- Дашборд показывает реальные счётчики из базы (организации, подразделения,
  контрагенты, проекты, сотрудники, открытые периоды). Финансовые
  показатели (ДДС, ОПиУ, баланс, маржинальность, точка безубыточности)
  сознательно не выводятся статичными значениями — они появятся на Этапе 3,
  когда будут построены начисления и банковские операции.

## Реализовано (Этап 2: Начисления и деньги)

- Документы начисления (`/accruals`): все 9 типов из ТЗ (счёт, акт, УПД,
  накладная, реализация, поступление, возврат, корректировка, ручное),
  многострочные, с аналитикой (подразделение, ЦФО, проект, продукт/услуга,
  статья ОПиУ) на уровне строки. Жизненный цикл черновик → проведён →
  отменён; редактировать можно только черновик; проведение и отмена
  проверяют, что учётный период открыт.
- Банковские счета и кассы как справочники; операции (`/cash/transactions`)
  вводятся вручную или загружаются из выписки.
- Универсальная загрузка банковской выписки (`/cash/import`): XLSX, XLS,
  CSV, TXT (TXT — автоопределение разделителя), ручное сопоставление
  колонок файла с полями операции (дата; сумма со знаком ИЛИ отдельные
  колонки прихода/расхода; назначение платежа; ИНН и наименование
  контрагента; номер операции банка), защита от повторной загрузки по
  fingerprint (композитный ключ счёт+дата+направление+сумма+назначение,
  либо номер операции банка, если он есть в файле), автоматическое
  определение контрагента по ИНН, протокол импортированных/дублей/ошибок.
  ~~Известное ограничение: без номера операции банка две операции с
  абсолютно одинаковыми счётом/датой/суммой/назначением могут быть
  ошибочно признаны дублем.~~ Исправлено — см. дополнение «Защита от
  дублей выписки без номера операции банка» в конце файла.
- Сопоставление банковских операций с начислениями (`/cash/transactions/[id]`):
  один платёж — один документ, один платёж — несколько документов
  (повторным сопоставлением остатка), несколько платежей — один документ;
  отмена сопоставления. Суммы считаются через `decimal.js`, без ошибок
  плавающей точки. Статусы оплаты документа (не оплачено / частично /
  оплачено / переплата) и сопоставления операции (не сопоставлено /
  частично / сопоставлено) пересчитываются автоматически.
- Дебиторская и кредиторская задолженность (`/reports/debts`): считается из
  проведённых непогашенных документов минус сопоставленные оплаты, с
  выделением просрочки по сроку оплаты.
- Платёжный календарь (`/payment-calendar`): текущий остаток по всем счетам
  и кассам (факт из банковских операций) + прогноз по срокам оплаты
  непогашенных начислений и согласованных заявок.
- Заявки на оплату (`/payment-requests`) с одноступенчатым согласованием
  (на согласовании → согласована/отклонена → оплачена), не полным
  настраиваемым маршрутом из раздела 9 ТЗ — это упрощение, отмеченное как
  ограничение ниже.
- Дашборд обновлён реальными показателями: остаток денег, дебиторка/
  кредиторка, просрочка, несопоставленные операции, согласованные заявки.

## Реализовано (Этап 3: Управленческая отчётность)

Всё в `src/lib/reports/` — чистые вычисления поверх сохранённых документов
и операций (без статичных цифр), с юнит-тестами на самую рискованную
арифметику (P&L-водопад, точка безубыточности, decimal-суммы).

- **ДДС** (`/reports/cash-flow`) — остаток на начало/конец периода,
  поступления и выплаты по статьям, внутренние переводы отдельной строкой.
  Остаток на начало периода считается как сумма всех операций до начала
  периода (а не через отдельно хранимый остаток на счёте).
- **ОПиУ** (`/reports/pnl`) — выручка → валовая прибыль → операционная
  прибыль → чистая прибыль по методу начисления, с разбивкой по статьям и
  сравнением с предыдущим месяцем. Считается только из проведённых
  документов.
- **Управленческий баланс** (`/reports/balance`) — денежные средства и
  задолженность из фактических данных; капитал/займы/авансы не
  отслеживаются отдельно (см. ограничение ниже) и приняты равными нулю.
  **Контрольное равенство**: «Активы − Обязательства» сверяется с
  накопленной чистой прибылью по ОПиУ, расхождение показывается как
  диагностика с объяснением вероятной причины, а не скрывается.
- **Маржинальность и точка безубыточности** (`/reports/margin`) — валовая
  прибыль, точка безубыточности (Постоянные затраты / Доля маржинального
  дохода), запас финансовой прочности, разбивка по проектам, продуктам/
  услугам и клиентам. Распределение косвенных расходов по этим разрезам —
  настраиваемый драйвер, см. дополнение после Этапа 7 ниже.
- **Фильтры**: период (год/месяц), организация, подразделение, ЦФО,
  проект, продукт/услуга, контрагент — на каждом отчёте.
- **Расшифровка до документа**: клик по статье в ДДС/ОПиУ открывает список
  операций/документов, отфильтрованный именно по этой статье и периоду.
- **Excel-экспорт** (`/api/reports/export`) — ДДС, ОПиУ, задолженность,
  управленческий баланс. Экспорт применяет то же ограничение видимости по
  организациям/подразделениям/проектам, что и сами отчёты на экране.
- Дашборд дополнен показателями текущего месяца: выручка, валовая/
  операционная/чистая прибыль, рентабельность.

## Не реализовано / упрощено (осознанно, по этапам ТЗ)

- ~~Импорт/экспорт Excel для справочников master-data (раздел 5 ТЗ) —
  только ручной CRUD.~~ Реализовано — см. дополнение «Импорт и экспорт
  справочников в Excel» в конце файла.
- Загрузка реквизитов контрагента по ИНН (раздел 6).
- Настраиваемый многошаговый маршрут согласования заявок на оплату
  (раздел 9 ТЗ) — см. дополнение после Этапа 7 ниже. Заявка без
  подходящего маршрута по-прежнему идёт одной ступенью.
- Автоматическое определение контрагента по ИНН при импорте — есть с
  самого начала. Автоклассификация статьи ДДС/подразделения/ЦФО/проекта/
  продукта по правилам — см. дополнение после Этапа 7 ниже.
- Перевод между собственными счетами (форма «Банк и касса → Добавить
  операцию», чекбокс «Перевод между собственными счетами» + выбор второго
  счёта/кассы) создаёт обе ноги атомарно: списание на исходном счёте и
  поступление на втором на ту же сумму, связанные `transferGroupId`
  (`src/lib/cash/transfer.ts`). Общий остаток по всем счетам больше не
  искажается, если ввести только одну сторону перевода — система требует
  указать оба счёта и не даёт выбрать один и тот же счёт дважды. На странице
  операции обе ноги ссылаются друг на друга. **Не реализовано**: редактирование
  и удаление уже созданной пары синхронно (изменить/удалить можно только
  отдельную ногу вручную, как любую другую операцию) — пар удаления
  банковских операций в системе пока в принципе нет ни для одной операции.
- Управленческий баланс не выделяет капитал, займы, авансы выданные/
  полученные и прочие активы отдельно от денег и задолженности — для
  этого нужна полноценная двойная запись или ручной ввод остатков,
  которых пока нет. Контрольное равенство именно поэтому может показывать
  расхождение — это диагностика, а не баг.
- План-факт и прогноз в ДДС/ОПиУ появятся вместе с финансовым
  моделированием (Этап 6).
- Банковские реквизиты и контакты контрагента редактируются в карточке
  контрагента — см. дополнение после Этапа 7 ниже.

## Структура

```
prisma/schema.prisma          — полная схема данных
prisma/seed.ts                 — роли, права, начальные справочники, админ
src/lib/                       — сессии, RBAC, аудит, периоды, деньги (decimal),
                                  сопоставление платежей, платёжный календарь
src/lib/dictionaries/          — конфигурация универсального CRUD-движка
                                  для однотипных справочников
src/lib/bank-import/           — парсер XLSX/XLS/CSV/TXT, сопоставление колонок,
                                  fingerprint для защиты от повторной загрузки
src/lib/accruals/              — подписи для типов/статусов документов начисления
src/lib/reports/               — расчётное ядро: ДДС, ОПиУ, баланс, маржинальность/ТБУ,
                                  дебиторка/кредиторка, фильтры, период, Excel-экспорт
src/app/login/                 — вход
src/app/(app)/                 — авторизованная часть (сайдбар + контент)
src/app/(app)/dashboard/       — дашборд
src/app/(app)/master-data/     — справочники (универсальный [slug]-роут)
src/app/(app)/accruals/        — документы начисления
src/app/(app)/cash/            — банк/касса, загрузка выписок, сопоставление
src/app/(app)/payment-requests/— заявки на оплату
src/app/(app)/payment-calendar/— платёжный календарь
src/app/(app)/reports/         — ДДС, ОПиУ, баланс, маржинальность/ТБУ, дебиторка/кредиторка
src/app/api/reports/export/    — Excel-экспорт отчётов (Route Handler)
src/lib/integrations/          — парсер/маппинг 1С, идемпотентный импорт, outbox Битрикс24
src/app/(app)/integrations/    — 1С: загрузка + журнал синхронизации; Битрикс24: очередь событий
src/app/api/integrations/1c/template/ — шаблон файла для 1С-импорта
src/lib/payroll/                — подписи, расчёт зарплаты (авансы/финал/налоги/распределение)
src/app/(app)/employees/        — сотрудники, кадровая история, распределение по проектам
src/app/(app)/timesheet/        — табель
src/app/(app)/payroll/          — расчёты зарплаты, сводная ведомость
src/lib/financial-model/        — драйверы, расчётный движок прогноза (чистые функции)
src/app/(app)/financial-model/  — сценарии, редактор драйверов, прогноз, сравнение
src/lib/period-close/           — контрольный лист закрытия периода, снимок показателей
src/lib/crypto/                 — шифрование секретов интеграций (AES-256-GCM)
src/lib/flash.ts                — одноразовые httpOnly-сообщения (временный пароль и т.п.)
src/app/api/health/             — health-check для мониторинга
src/app/(app)/admin/           — пользователи, роли, периоды (+ закрытие), журнал аудита
Dockerfile, docker-compose.yml — контейнерная сборка (см. «Развёртывание через Docker»)
```

## Реализовано (Этап 4: Интеграции)

- **1С — файловый обмен** (`/integrations/1c`): импорт документов начисления
  из табличной выгрузки (XLSX/CSV/TXT — XML/JSON из ТЗ не поддержаны, см.
  ограничение ниже), ручное сопоставление колонок, шаблон файла для
  скачивания. **Идемпотентно**: строка с уже известным внешним ID 1С
  обновляет существующий документ, а не создаёт дубль — проверено
  сценарием «импорт → повторный импорт того же файла → импорт с изменённой
  суммой» с автоматической сверкой количества документов и итоговой суммы.
  Организация ищется по ИНН (создать нельзя — ошибка, если не найдена);
  контрагент ищется по ИНН и автоматически создаётся, если не найден
  (с пометкой `dataSource: "1C"`). Строка с датой в закрытом периоде или
  несуществующей организацией/статьёй не прерывает импорт — уходит в
  очередь ошибок с понятным сообщением, видна в журнале синхронизации
  (`/integrations/1c/batches/[id]`) вместе со счётчиками создано/
  обновлено/ошибок. Прямой обмен через HTTP/OData — не реализован
  (нет адреса публикации и учётных данных), как и требует ТЗ.
- **Битрикс24 — outbox** (`/integrations/bitrix24`): при каждом изменении
  статуса оплаты проведённого документа начисления (сопоставление платежа
  или отмена сопоставления) в очередь идемпотентно ставится событие
  `payment_status_changed` с суммой задолженности и признаком просрочки.
  idempotencyKey включает сумму сопоставления, поэтому повтор с тем же
  состоянием не создаёт дубль в очереди — проверено вручную. Ручная
  отправка через настраиваемый вебхук с журналом попыток и ошибок;
  без включённого профиля событие остаётся в очереди со статусом
  «Ожидает отправки», попытки не растрачиваются впустую. Реальная отправка
  проверена на неотвечающем URL: статус корректно переходит в «Ошибка»,
  попытка и текст ошибки записываются.

## Не реализовано / упрощено — Этап 4

- Импорт из 1С поддерживает только табличные форматы (XLSX/CSV/TXT).
  XML и JSON из раздела 10 ТЗ не поддержаны — формат 1С-выгрузки сильно
  зависит от конкретной конфигурации 1С, и без реального образца выгрузки
  надёжный парсер был бы угадыванием, а не рабочим кодом.
- Один файл 1С = один документ на строку с одной строкой начисления
  (без постатейной детализации внутри документа) — типично для
  табличных выгрузок 1С, но не покрывает многострочные документы.
- Прямая интеграция 1С (HTTP/OData/расширение) и реальная доставка в
  Битрикс24 не проверялись на живом сервере — нет доступов. Инфраструктура
  (профиль, конфиг, включение/выключение) готова, отправка кода написана
  и проверена на ошибке сети, но не на реальном вебхуке.
- Финансовый результат по сделке/проекту для Битрикс24 (раздел 11 ТЗ) —
  не реализован, только статус оплаты и задолженность.

## Реализовано (Этап 5: Кадры и зарплата)

- **Сотрудники** (`/employees`): приём, перевод (подразделение/должность —
  через отдельное действие, а не прямое редактирование, чтобы сохранялась
  кадровая история), увольнение — каждое событие пишется в
  `employment_history`. Распределение занятости по проектам в долях (%),
  используется при расчёте зарплаты для разнесения ФОТ.
- **Табель** (`/timesheet`): массовое заполнение по списку сотрудников и
  диапазону дат (рабочие/выходные/отпуск/больничный/командировка/
  отсутствие/сверхурочные/проектные часы), просмотр и удаление по
  сотруднику и месяцу.
- **Виды начислений и налоговые правила** — настраиваемые справочники
  (`/master-data/payroll-accrual-types`, `/master-data/tax-rules`):
  засеяны 10 видов из ТЗ (оклад, аванс, премия, проектная доплата,
  отпускные, больничные, компенсация, материальная помощь, разовая
  выплата, удержание) и стандартные ставки НДФЛ 13% / страховых взносов
  22+5,1+2,9+0,2% — все редактируемые через интерфейс, не зашиты в код.
- **Расчёт зарплаты** (`/payroll`): расчёт по организации со статусами
  черновик → рассчитан → утверждён → выплачен. Автоматический расчёт для
  видов «Аванс»/«Оклад» — аванс 25 числа = 50% оклада, окончательный
  расчёт 10 числа = оклад минус уже выплаченный аванс за предыдущий месяц
  (найден по календарному месяцу, а не по periodId — аванс и финальный
  расчёт относятся к разным учётным периодам). НДФЛ и страховые взносы
  считаются из настраиваемых ставок. Остальные виды начислений (премия,
  отпускные и т.д.) добавляются вручную построчно. Сумма одной строки
  автоматически разносится по проектным долям сотрудника
  (`PayrollAllocation`) или полностью на его подразделение, если долей
  нет. Разовые расчёты (ADHOC) — только ручные строки. Закрытый период
  блокирует создание, пересчёт и добавление строк.
- **Сводная ведомость** (`/payroll/summary`): на выбранную дату выплаты —
  к выплате наличными/безналично, НДФЛ, страховые взносы, итоговая
  денежная потребность, разбивка по организациям, подразделениям,
  проектам и сотрудникам. Экспорт в Excel (`/api/reports/export?type=payroll-summary`).

Проверено сквозным сценарием: принял сотрудника с окладом 100 000 ₽ →
аванс рассчитался как 50 000 ₽ (НДФЛ 6 500, взносы 15 100, к выплате
43 500) → утвердил → выплатил → сводная ведомость на эту дату показала
ровно эти цифры с разбивкой по организации/подразделению/сотруднику →
создал окончательный расчёт за следующий месяц — система корректно нашла
аванс за предыдущий месяц и посчитала остаток 50 000 ₽ с теми же
налогами. Тестовые данные удалены.

Попутно нашёл и исправил ещё три пробела в схеме Этапа 1 (аналогичные
багу с `PaymentRequest` на Этапе 2): `EmploymentHistory` и `TimeSheet`
имели поля-идентификаторы (`fromDepartmentId`, `toPositionId`,
`projectId` и т.д.) без соответствующих Prisma-связей, `BankImportBatch.
importedById` — так же. Добавлена миграция `payroll_hr_relations`.

## Не реализовано / упрощено — Этап 5

- Средний заработок для отпускных/больничных не рассчитывается
  автоматически по фактическим начислениям за расчётный период (12
  месяцев) — сумма вводится вручную построчно. Поле
  `affectsAvgEarnings` на виде начисления есть в схеме и настраивается,
  но пока не используется расчётной логикой.
- Табель не влияет на автоматический расчёт оклада (оклад считается
  как фиксированная доля от `Employee.salary`, а не пропорционально
  отработанным дням по табелю) — учтено только для отображения и
  проектных часов.
- Автопроводка начисленной зарплаты в ОПиУ — см. дополнение после
  Этапа 7 ниже.

## Реализовано (Этап 6: Финансовое моделирование)

- **Сценарии** (`/financial-model`): базовый/оптимистичный/пессимистичный/
  пользовательский, любое количество. Прогноз строится на 12 месяцев от
  выбранного старта (по умолчанию — текущий месяц), стартовая точка
  остатка денег — реальный фактический остаток по банковским/кассовым
  операциям на сегодня (не выдуманное число).
- **Драйверы**: средний чек, количество продаж, сезонность, активация
  новых услуг, доля продаж через посредников и их комиссия, переменные
  расходы (% от выручки), постоянные расходы, средняя стоимость
  сотрудника в месяц, платежи по кредитам/лизингу/отсрочкам — все
  редактируются помесячно в одной таблице-редакторе. Пустая ячейка
  означает «не задано», а не 0: для сезонности и активации новых услуг
  это трактуется как 100% (по умолчанию не искажает прогноз), для
  остальных драйверов — как 0.
- **Производительность на сотрудника отдельно по подразделениям**
  (обязательное требование ТЗ, минимум для отдела обучения и охраны
  труда — эти два подразделения подставляются в редактор по названию,
  если их переименовали — берутся первые доступные): для каждого
  подразделения задаются продажи и производительность на сотрудника,
  система считает **требуемую численность = продажи / производительность
  (с округлением вверх)** и суммирует с вручную заданной численностью
  прочего персонала.
- **Прогноз**: выручка → переменные расходы → комиссия посредников →
  валовая прибыль → постоянные расходы + ФОТ → операционная прибыль,
  точка безубыточности и запас финансовой прочности (переиспользуют
  формулы Этапа 3), остаток денег помесячно нарастающим итогом с учётом
  платежей по кредитам.
- **Сравнение сценариев** (`/financial-model/compare`): любое количество
  сценариев бок о бок — выручка, операционная прибыль, остаток денег на
  конец горизонта, средняя точка безубыточности за 12 месяцев.

Проверено сквозным сценарием через реальный UI (не напрямую через БД):
создал сценарий → ввёл средний чек 1000, продажи 100, переменные 30%,
постоянные 20 000, продажи/производительность по двум подразделениям
(55/20 и 40/20) → сохранил → прогноз показал ровно то, что предсказывает
формула: выручка 100 000, численность 5 (3+2, округление вверх),
операционная прибыль −25 000, точка безубыточности 135 714,29, запас
прочности −35,71% — до копейки. Сравнение двух сценариев отобразило
правильные значения бок о бок. Тестовые данные удалены.

При проверке через браузер нашёл и исправил баг в самом тестовом
сценарии использования (не в приложении): `document.querySelector('form')`
в моём тестовом JS-скрипте попадал на форму выхода из системы в шапке
(она в DOM раньше формы страницы) и разлогинивал сессию вместо создания
сценария — после уточнения селектора (`input.closest('form')`) всё
заработало. Также обнаружил и исправил реальный баг в схеме: Prisma не
поддерживает `null` в составном уникальном ключе (`FinancialScenarioValue`
на `[scenarioId, year, month, driver, dimension]`) — заменил
`dimension String?` на `dimension String @default("")` с пустой строкой
как сентинел для «без подразделения» (миграция
`financial_scenario_dimension_default`).

## Не реализовано / упрощено — Этап 6

- Драйвер «активация новых услуг» — общий множитель на выручку, а не
  список конкретных услуг с датой запуска по месяцам (раздел 15 ТЗ хочет
  именно список).
- Остаток денег в прогнозе = предыдущий остаток + операционная прибыль −
  платежи по кредитам — упрощённая модель без разнесения по срокам
  оплаты (в отличие от факта в платёжном календаре, Этап 2). Кредит/лизинг
  учтён только как отток денег, без графика начисления процентов.
- Добавление ещё одного подразделения для отдельной производительности
  (сверх показанных 2–6) требует прямого редактирования данных — в
  интерфейсе нет кнопки «добавить подразделение» в редакторе сценария.

## Реализовано (Этап 7: Закрытие месяца и production-готовность)

### Контрольный лист закрытия периода

`/admin/periods/[id]/close` — все 9 проверок из раздела 17 ТЗ, реально
запрашивают базу (не заглушки):

1. Необработанные документы 1С (импорты с ошибками) — **критично**
2. Ошибки и конфликты интеграций — **критично**
3. Документы без обязательной аналитики (строка начисления без статьи
   ОПиУ) — **критично**
4. Несопоставленные банковские операции за период — предупреждение
5. Незавершённые зарплатные расчёты за период (не «утверждён»/«выплачен»)
   — **критично**
6. Несогласованные заявки на оплату со сроком в периоде — предупреждение
7. Расхождения управленческого баланса (контрольное равенство из Этапа 3)
   — предупреждение
8. Непроведённые (черновик) документы начисления за период —
   предупреждение
9. Дубли банковских операций по отпечатку — предупреждение (структурно
   не должно случаться, т.к. защищено уникальным индексом при загрузке)

Критические ошибки **блокируют** закрытие — кнопки «Закрыть период» нет,
пока они не исправлены. Предупреждения требуют явной галочки «Я
ознакомился и осознанно закрываю период». Результаты каждой проверки
сохраняются в `period_close_checks` при каждой попытке закрытия (даже
неудачной) — это часть истории периода, видна в списке периодов рядом с
датой закрытия.

При успешном закрытии фиксируется **снимок контрольных показателей**
(`AccountingPeriod.closingSnapshot`): остаток денег на начало/конец
периода, выручка, чистая прибыль, дебиторка, кредиторка, активы,
обязательства, расхождение баланса — на момент закрытия, не
пересчитывается задним числом при последующих изменениях. Показан в
списке периодов по клику.

Закрытый период запрещает изменение документов везде, где это уже было
реализовано на предыдущих этапах (`assertPeriodOpenForDate` в
начислениях, банковских операциях, зарплате) — проверено вживую: критичная
ошибка (незавершённый расчёт зарплаты) корректно заблокировала закрытие
периода без кнопки закрытия.

### Безопасность

Заказал отдельный агентский аудит безопасности по чек-листу раздела 22 ТЗ
(RBAC на каждом действии, SQL-инъекции, XSS, CSRF, секреты в коде,
безопасность загрузки файлов, утечка технических деталей в ошибках,
конфигурация cookie). Нашлось два реальных замечания, оба исправлены:

- **Временный пароль передавался в query-параметре URL** при создании
  пользователя/сбросе пароля — оседал в логах доступа, истории браузера,
  заголовке Referer. Заменено на одноразовую httpOnly-куку
  (`src/lib/flash.ts`, TTL 60 секунд), пароль в URL больше не попадает.
- **Вебхук Битрикс24 хранился в БД открытым текстом** (в его URL зашит
  токен доступа) — теперь шифруется AES-256-GCM с ключом, производным от
  `AUTH_SECRET` (`src/lib/crypto/secret-box.ts`), в форме показывается
  только маска (первые 8 и последние 4 символа).

Остальные пункты чек-листа (RBAC на каждом действии, безопасность
загрузки файлов, отсутствие сырых SQL-конкатенаций, отсутствие секретов в
репозитории, конфигурация cookie, отсутствие `dangerouslySetInnerHTML`)
прошли проверку без замечаний — подробности в истории коммита.

### Мониторинг

`GET /api/health` — без аутентификации, проверяет доступность БД
(`SELECT 1`), отдаёт `{status, database, time}` с кодом 503 при
недоступности. Подключается к любому внешнему аптайм-чекеру.

### Резервное копирование

Автоматического расписания бэкапов в этой версии нет (нет постоянной
инфраструктуры для крон-задачи в среде разработки). Документирована
ручная процедура:

```bash
# бэкап
pg_dump -U promtehnosfera -h localhost promtehnosfera | gzip > backup_$(date +%Y%m%d).sql.gz
# восстановление
gunzip -c backup_20260922.sql.gz | psql -U promtehnosfera -h localhost promtehnosfera
```

### Производительность

Добавлены индексы под реальные запросы отчётов и контрольного листа:
`AccrualDocument(status, paymentStatus)`, `BankTransaction(matchStatus)`,
`PaymentRequest(dueDate)`, `PaymentRequest(status)`, `PayrollRun(payoutDate)`
(миграция `period_close_snapshot_and_indexes`).

### Документация и развёртывание

`Dockerfile` (standalone-сборка Next.js) и `docker-compose.yml`
(приложение + PostgreSQL) — см. раздел «Развёртывание через Docker» выше.

## Не реализовано / упрощено — Этап 7

- Резервное копирование не автоматизировано (нет крона/расписания) —
  только документированная ручная команда.
- Docker-конфигурация не запускалась в этой среде (Docker не установлен) —
  написана по стандартным практикам (multi-stage build, non-root user,
  standalone output), но не проверена сборкой образа.
- Контрольный лист проверяет закрытый период целиком по календарному
  месяцу; не проверяет полноту первичных документов там, где это требует
  доменных знаний за пределами схемы (например, «все ли реализации за
  месяц действительно занесены» — система не может знать о документах,
  которых в ней нет).
- Нет отдельного постоянного мониторинга (метрики, алерты) — только
  один health-check эндпоинт для внешнего аптайм-чекера.

## Итоговый статус

Все 7 этапов ТЗ реализованы поверх реальной схемы PostgreSQL, без
демонстрационных данных на экране — каждое число в интерфейсе читается
из сохранённых записей. Критерии приёмки первой рабочей версии (раздел 27
ТЗ) выполнены и проверены вручную в браузере в ходе разработки: создание
организации/подразделения/ЦФО/проекта/услуги/контрагента, начисление
дохода и расхода, загрузка банковской выписки произвольного формата,
сопоставление платежа с начислением (полное и частичное), корректная
дебиторка/кредиторка, ДДС и ОПиУ из реальных данных, сходящийся
управленческий баланс с диагностикой расхождений, фильтры по
организации/подразделению/проекту/услуге, расшифровка показателей до
документов, идемпотентный импорт из 1С без дублей, контроль закрытия
месяца, закрытие периода с запретом изменений, выгрузка отчётов в Excel.

141 автотест (`npm test`), production-сборка проходит (`npm run build`).
Список того, что осознанно осталось упрощением или заглушкой на каждом
этапе, — в соответствующих разделах выше; каждый такой пункт — сознательное
решение по объёму работы, а не скрытая недоделка.

## Дополнение после Этапа 7: видимость по организациям/подразделениям/проектам

Реализовано разграничение видимости данных по организациям, подразделениям и
проектам (`src/lib/access-scope.ts`, `src/app/(app)/admin/users/actions.ts`,
UI — «Администрирование → Пользователи → [пользователь] → Ограничение
видимости»). Это доводит до конца пункт, заложенный в схему ещё на Этапе 1
(`UserOrganizationAccess` и т.д.), но не подключённый к фильтрации до этого
момента. Изначально экспорт в Excel был не подключён к этому ограничению —
теперь подключён (см. дополнение ниже). Осталось не подключено: снимки
закрытия периода и выпадающие списки в формах создания документов.

## Дополнение после Этапа 7: перевод между собственными счетами

Перевод между собственными счетами теперь создаёт обе ноги проводки
атомарно (`src/lib/cash/transfer.ts`, `createBankTransactionAction`),
закрывая пункт из раздела «Банк и касса» выше. Подробности там же.

## Дополнение после Этапа 7: экспорт управленческого баланса и сводной ведомости зарплаты в Excel

`/api/reports/export` дополнен типами `balance` и `payroll-summary` —
закрывает два отдельных пункта («Экспорт управленческого баланса в Excel не
реализован» из раздела «Управленческая отчётность» и «Экспорт сводной
ведомости в Excel не реализован» из раздела «Кадры и зарплата»). Заодно
экспорт ДДС/ОПиУ/задолженности/баланса теперь учитывает ограничение
видимости по организациям/подразделениям/проектам — раньше эндпоинт
экспорта не знал о нём вовсе. Сводная ведомость зарплаты вынесена в чистую
переиспользуемую функцию `computePayrollSummary` (`src/lib/payroll/summary.ts`)
с юнит-тестами, вместо инлайновой агрегации прямо в странице.

## Дополнение после Этапа 7: экспорт прогноза финансового сценария в Excel

`/api/reports/export` дополнен типом `scenario-forecast` — экспортирует
12-месячный прогноз сценария (выручка, расходы, прибыль, требуемая
численность, точка безубыточности, остаток денег) той же структурой, что
показана на экране. Текущий фактический остаток денег — стартовая точка
прогноза — вынесен в переиспользуемую `getCurrentCashBalance`
(`src/lib/financial-model/current-cash.ts`) и теперь тоже учитывает
ограничение видимости по организациям (раньше страница сценария вообще не
знала о нём). При разработке экспорт сразу же поймал реальный баг: имя
файла в `Content-Disposition` собиралось из названия сценария как есть, а
HTTP-заголовки — ASCII/Latin1, поэтому кириллица в названии (обычный случай
для этой системы) валила запрос 500-й ошибкой; исправлено транслитерацией
недопустимых символов в `_`.

## Дополнение после Этапа 7: автоклассификация банковских операций по правилам

Реализовано правило-based автоматическое назначение статьи ДДС (и,
опционально, подразделения/ЦФО/проекта/продукта-услуги) банковским
операциям — раздел «Администрирование → Правила классификации»
(`/cash/classification-rules`, доступ по `CASH_MANAGE`/`CASH_VIEW`).

- Правило срабатывает, если совпадают ВСЕ заданные условия: направление
  (опционально), вхождение подстроки в назначение платежа, точный ИНН
  контрагента, точная сумма. Правило без единого условия (кроме
  направления) создать нельзя — иначе оно молча классифицировало бы вообще
  всё. Правила проверяются по возрастанию `priority`, побеждает первое
  совпавшее. Чистая логика — `src/lib/bank-import/classification.ts`,
  11 юнит-тестов.
- Применяется в двух местах: (1) автоматически при загрузке банковской
  выписки (`importBankStatementAction`) — сразу после автоопределения
  контрагента по ИНН; (2) вручную, кнопкой «Применить правила к
  несопоставленным операциям» на странице правил — для уже загруженных
  операций без статьи ДДС (ручную классификацию не трогает и не
  перезаписывает).
- Новая таблица `BankClassificationRule` в схеме (миграция
  `add_bank_classification_rules`).
- **Не реализовано**: защита от повторной загрузки по-прежнему не
  использует правила (это отдельный, уже существующий механизм —
  `fingerprint`); правила не участвуют в определении дублей.

## Дополнение после Этапа 7: настраиваемый многошаговый маршрут согласования заявок на оплату

Реализован раздел «Администрирование → Маршруты согласования»
(`/admin/payment-approval-routes`) — закрывает пункт «Заявка на оплату
проходит одноступенчатое согласование» из раздела 9 ТЗ.

- Маршрут — упорядоченный список шагов, каждый шаг требует согласования от
  конкретной **роли** (не пользователя — так маршрут не привязан к
  конкретному человеку). Маршрут применяется к заявке, если сумма попадает
  в заданный диапазон (границы включительно, пусто — без ограничения) и
  организация совпадает (пусто — любая); среди подходящих действует
  наименьший приоритет. Заявка без подходящего маршрута идёт по старому
  одноступенчатому согласованию (`payment_requests.approve`) — так
  включение функции не меняет поведение системы, пока администратор явно
  не создаст маршрут. Выбор маршрута делается один раз при создании
  заявки и хранится на ней (`routeId`) — правки маршрута после создания
  заявки на уже созданные заявки не переносятся сами по себе, но **правки
  списка шагов существующего маршрута повлияют на заявки, уже идущие по
  нему** (шаг может исчезнуть или роль — измениться), это явно
  зафиксировано как ограничение на странице редактирования маршрута.
- Согласование шага требует членства именно в роли этого шага (проверяется
  прямым запросом к `UserRole`, не через `session.permissions`) — так
  «Руководитель подразделения» может согласовать свой шаг, не имея общего
  права `payment_requests.approve`. Обход — `admin.full`, как везде в
  системе. Заявка без маршрута по-прежнему требует общего права.
- Чистая логика выбора маршрута и шагов — `src/lib/payment-requests/approval.ts`,
  9 юнит-тестов.
- **Не реализовано**: комментарий согласующего к решению (поле `comment` в
  `PaymentRequestApproval` осталось от предыдущей версии, но шаговое
  согласование его пока не заполняет — только фиксирует решение и номер
  шага); история решений по шагам не выведена в интерфейс (только текущий
  шаг), хотя и пишется в БД и в журнал аудита.

## Дополнение после Этапа 7: автопроводка начисленной зарплаты в ОПиУ

При утверждении расчёта зарплаты (кнопка «Утвердить», переход
`CALCULATED → APPROVED`) система теперь автоматически создаёт и сразу
проводит документ начисления (расход) на сумму начислено + страховые
взносы по каждой строке расчёта, чья статья начисления привязана к
статье ОПиУ (справочник «Виды начислений зарплаты» — поле «Статья
расходов», было в схеме с самого начала, но раньше ничего не читало эту
связь). Закрывает пункт из раздела «Кадры и зарплата» — модуль зарплаты
был отдельным от начислений.

- Документу нужен контрагент (обязательное поле схемы), а сотрудники —
  не контрагенты; заведена единственная синтетическая запись-клиринг
  «Сотрудники (ФОТ)», общая на всю систему, переиспользуемая для всех
  организаций и расчётов.
- Начисленная сумма и страховые взносы относятся на одну и ту же статью
  ОПиУ вида начисления — отдельной статьи «страховые взносы» в этой
  версии нет, оба вида затрат считаются частью «Расходы на оплату
  труда» (сознательное упрощение).
- Идемпотентно: связь документа с расчётом — через `(sourceSystem,
  externalId) = ("payroll", payrollRunId)`, повторное утверждение (если
  бы оно было возможно) не создаст второй документ.
- Строки расчёта без привязанной статьи ОПиУ пропускаются — по ним, как
  и раньше, начисление остаётся ручным; если ни одна строка не
  привязана, документ вообще не создаётся, и страница расчёта явно
  объясняет, почему.
- ДДС отдельно ничего не автоматизирует — как только фактическая
  выплата зарплаты будет сопоставлена с банковской операцией (обычный
  механизм сопоставления платежей, тот же, что для любого документа
  начисления), она попадёт в ДДС сама, без дополнительного кода.
- Чистая логика построения строк — `src/lib/payroll/post-to-accrual.ts`
  (`buildPayrollAccrualLines`), 5 юнит-тестов.
- Проверено сквозным сценарием: сотрудник с окладом 100 000 ₽ → аванс
  50 000 ₽ (НДФЛ 6 500, взносы 15 100) → рассчитан → утверждён →
  автоматически создан и проведён документ «Ручное начисление» на
  65 100 ₽ (= 50 000 + 15 100) с контрагентом «Сотрудники (ФОТ)» →
  отчёт ОПиУ немедленно показал эту сумму по статье «Расходы на оплату
  труда» и уменьшил прибыль на ту же сумму.

## Дополнение после Этапа 7: настраиваемый драйвер косвенных расходов в маржинальности

Раньше страница «Маржинальность и ТБУ» заявляла, что косвенные расходы
распределяются пропорционально выручке, но фактически таблицы по
проектам/продуктам/клиентам показывали только валовую прибыль — косвенные
расходы в разрезы не распределялись вовсе. Теперь:

- В каждой таблице разреза добавлены колонки «Косвенные (аллокация)»,
  «Операционная прибыль», «Опер. маржинальность».
- Драйвер выбирается в фильтре отчёта: пропорционально выручке (по
  умолчанию), пропорционально валовой прибыли, поровну. Строки с
  нулевым/отрицательным весом не получают отрицательную долю; если вес
  нулевой у всех строк — сумма делится поровну, чтобы не потеряться.
  Чистая функция `allocateIndirectCosts` (`src/lib/reports/margin.ts`),
  7 юнит-тестов.
- Распределяется общая сумма косвенных расходов периода (с учётом
  фильтров отчёта) между строками, у которых есть выручка или прямые
  затраты в этом разрезе.
- Проверено в браузере на проведённых документах: проект A (выручка
  200 000, прямые затраты 50 000), проект B (выручка 100 000), косвенные
  расходы 60 000 без привязки к проекту. Драйвер «по выручке» дал A 40 000 /
  B 20 000 косвенных (операционная прибыль 110 000 / 80 000), «по валовой
  прибыли» — 36 000 / 24 000 (114 000 / 76 000), «поровну» — 30 000 / 30 000
  (120 000 / 70 000). Всё совпало с ручным расчётом, общая операционная
  прибыль при любом драйвере — 190 000. Драйвер переключается через
  выпадающий список в фильтре, на остальных отчётах этого поля нет.

## Исправление: создание проекта в справочнике падало с ошибкой 500

При создании проекта без явного выбора статуса форма отправляла
`status: null`, а колонка `Project.status` не допускает null (у неё
значение по умолчанию `active`). Исправлено двумя способами: у поля
статуса проекта задано значение по умолчанию, а общая логика
справочников (`src/app/(app)/master-data/actions.ts`) больше не
отправляет `null` для незаполненных необязательных полей — поле просто
не передаётся, и срабатывает значение по умолчанию из БД. Проверено в
браузере: проекты создаются.

## Дополнение после Этапа 7: банковские реквизиты и контакты контрагента

Таблицы `CounterpartyBankDetail` и `CounterpartyContact` были в схеме с
Этапа 1, но в интерфейсе редактировались только основные поля
контрагента. Теперь на странице «Справочники → Контрагенты → Изменить»
под основной формой есть два блока — «Банковские реквизиты» и
«Контакты» — со списком, удалением и формой добавления (право
`masterdata.manage`, каждое действие пишется в журнал аудита).

- Проверка реквизитов: расчётный и корреспондентский счёт — 20 цифр,
  БИК — 9 цифр; пробелы из номеров убираются автоматически
  («4070 2810 …» сохраняется как «40702810…»). У контакта обязательно
  имя, email проверяется на формат. Чистые функции
  `src/lib/counterparties/validation.ts`, 8 юнит-тестов.
- Проверено в браузере: неверный БИК и некорректный email отклоняются с
  понятным сообщением и ничего не сохраняют; корректные реквизиты и
  контакт сохраняются (пробелы из счёта убраны); удаление работает.
- **Не реализовано**: редактирование уже сохранённой строки — её можно
  удалить и добавить заново; отметка «основной счёт» для контрагента с
  несколькими счетами.

## Дополнение после Этапа 7: импорт и экспорт справочников в Excel

Каждый справочник («Справочники → [справочник]») получил кнопки «Экспорт в
Excel» (все, кто видит справочник) и «Импорт из Excel» (право
`masterdata.manage`). Закрывает пункт раздела 5 ТЗ.

- Экспорт (`/api/master-data/export?slug=…`) выгружает все записи, включая
  архивные, в XLSX. Заголовки — подписи полей формы, значения списков —
  названия, как в интерфейсе (не внутренние ID), флажки — «да»/«нет», даты —
  ГГГГ-ММ-ДД. Поэтому выгрузка одновременно служит шаблоном для импорта:
  её можно дополнить строками и загрузить обратно.
- Импорт принимает XLSX, XLS, CSV (до 8 МБ и 5000 строк). Колонки
  сопоставляются с полями по названию (регистр и «*» не важны, лишние
  колонки игнорируются). Ссылки на другие справочники (организация проекта,
  контрагент договора и т.п.) указываются названием записи. Даты — ГГГГ-ММ-ДД
  или ДД.ММ.ГГГГ, числа — с пробелами и запятой («1 500,50»).
- **Всё или ничего**: сначала проверяются все строки, и при любой ошибке не
  создаётся ни одной записи — страница показывает список ошибок с номерами
  строк файла («Строка 3: «АО» — нет такого значения для поля «Тип»»).
  Иначе все записи создаются одной транзакцией, в журнал аудита пишется
  имя файла и число созданных записей.
- Если в справочнике есть две записи с одинаковым названием (например, две
  организации «ООО Ромашка»), ссылка на это название считается ошибкой, а
  не молча привязывается к первой попавшейся.
- Чистая логика — `src/lib/dictionaries/spreadsheet.ts`, 9 юнит-тестов.
- Попутно исправлены три бага общего разборщика файлов
  (`src/lib/bank-import/parser.ts`), которым пользуются также загрузка
  банковской выписки и файловый обмен с 1С (6 юнит-тестов):
  1. CSV без BOM в UTF-8 или в Windows-1251 (обычная кодировка выгрузок
     российских банков и 1С) читался как Latin-1 — кириллица превращалась в
     «кракозябры». Теперь кодировка определяется автоматически.
  2. Дата «01.09.2026» в CSV распознавалась как американская (9 января), а
     ИНН с ведущим нулём (например, 0274…) превращался в число и терял ноль.
     Теперь ячейки CSV читаются как текст, даты и суммы разбирает код
     системы.
  3. Ячейка Excel с форматом даты переводилась в дату по местному часовому
     поясу (для Москвы ещё и с историческим смещением) и после этого
     сдвигалась на день назад: 01.09.2026 → 2026-08-31. Теперь номер дня
     Excel переводится в календарную дату напрямую, без часовых поясов
     (тест проверен в поясах Москвы, Нью-Йорка и Токио).
- Проверено в браузере: CSV с кириллицей → 2 организации; файл с ошибками →
  список ошибок, ни одной записи; экспорт → корректный XLSX, который
  загружается обратно без правок; проекты со ссылкой на организацию по
  названию, пустым статусом (подставился «Активный») и датой «01.09.2026»
  (в БД — 2026-09-01); неоднозначная ссылка на организацию отклонена.
- **Не реализовано**: импорт только добавляет записи — обновление
  существующих по ключу и проверка на дубли (повторная загрузка того же
  файла создаст записи повторно) не делаются; банковские реквизиты и
  контакты контрагента в импорт/экспорт не входят.

## Дополнение после Этапа 7: защита от дублей выписки без номера операции банка

Если в выписке нет номера операции банка, строка опознаётся по счёту,
дате, направлению, сумме и назначению платежа. Раньше две настоящие, но
одинаковые операции (например, две комиссии по 150 ₽ за перевод в один
день) давали один и тот же отпечаток — вторая молча считалась дублем и
терялась. Теперь отпечатки считаются по всему файлу сразу, и одинаковые
строки нумеруются в порядке появления:

- первая получает прежний отпечаток — поэтому уже загруженные ранее
  операции и операции, введённые вручную, по-прежнему опознаются как
  дубли; вторая — отпечаток с пометкой «#2», третья — «#3» и т.д.;
- повторная загрузка того же файла даёт те же номера — все строки
  отсекаются как дубли;
- выписка, перекрывающая уже загруженную (например, сначала выписка за
  часть дня, потом за весь день), тоже обрабатывается верно: известные
  операции отсекаются, новые одинаковые загружаются, порядок остальных
  строк в файле не важен;
- строки с номером операции банка не нумеруются: один номер дважды — это
  одна и та же операция.

Протокол загрузки сообщает, сколько строк загружено как повторы уже
встречавшейся в файле операции. Чистая логика —
`computeStatementFingerprints` (`src/lib/bank-import/fingerprint.ts`),
6 юнит-тестов.

Проверено в браузере: выписка с двумя одинаковыми комиссиями и платежом →
загружено 3 (раньше было бы 2 и «1 дубль»); та же выписка повторно →
0 загружено, 3 дубля; выписка за полный день с третьей такой же комиссией
и новой арендой (в другом порядке строк) → 2 загружено, 3 дубля. В базе —
ровно 3 комиссии, платёж и аренда, даты верные.

- **Ограничение**: нумерация опирается на то, что каждая выписка содержит
  день целиком или его начало. Если загрузить сначала выписку, где из двух
  одинаковых операций дня есть только вторая (банк выгрузил день не с
  начала), а затем полную — первая из них будет признана дублем. На
  практике банки выгружают выписки целыми днями.
- Ручной ввод операции (`/cash/transactions/new`) по-прежнему не даёт
  ввести вторую полностью одинаковую операцию в тот же день на тот же
  счёт — там это защита от двойного нажатия; при необходимости достаточно
  чуть изменить назначение платежа.
